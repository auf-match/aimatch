# Не тащим в build-контекст лишнее и тяжёлое.
node_modules
.next
.git
.env
uploads
# 94MB Huntflow-экспорт с PII — никогда в образ.
# ВАЖНО: в .dockerignore комментарий в конце строки не работает —
# вся строка читается как паттерн, поэтому пишем его отдельной строкой.
scripts/data
docs
*.md
.DS_Store
.superpowers
dist
*.tsbuildinfo
