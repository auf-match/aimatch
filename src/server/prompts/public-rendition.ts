import type { PublicResult } from "@/lib/public-result";

/**
 * Переложение разбора на прямое обращение к человеку.
 *
 * Внутренний разбор написан для дизайн-лида и говорит о кандидате в
 * третьем лице: «портфолио сильного дизайнера», «роль не указана». Таким
 * он и должен остаться — это рабочий документ. А человеку, который читает
 * про себя, нужно обращение напрямую.
 *
 * На вход подаём УЖЕ отфильтрованный публичный результат, а не сырой
 * разбор. Так внутренние стоп-факторы не попадут в переложение даже по
 * ошибке модели: их просто нет во входных данных.
 */
export function buildPublicRenditionPrompt(результат: PublicResult): string {
  return `Ты редактор. Перепиши разбор портфолио так, чтобы он обращался к автору напрямую.

ЧТО СДЕЛАТЬ
Третье лицо переведи во второе: «портфолио сильного дизайнера» → «у тебя сильное портфолио», «роль не указана» → «ты не указал свою роль». Обращайся на «ты».

ДЛИНА
Каждое пояснение — не длиннее 200 знаков. Это не пожелание: карточка на экране рассчитана ровно под столько. Если в исходном тексте больше, сократи его, оставив главное, а не обрывай на полуслове.

ЧЕГО НЕ ДЕЛАТЬ
- Не меняй смысл и не смягчай оценку. Если написано, что чего-то не хватает, — так и напиши, только обращаясь к человеку.
- Ничего не добавляй от себя: ни советов, ни похвал, ни выводов, которых нет в исходном тексте.
- Не выбрасывай пункты и не объединяй их. Сколько на входе — столько на выходе.
- Не трогай названия критериев и ступени, они передаются отдельно.
- Без обращений по имени, без приветствий и прощаний, без «надеюсь, это поможет».

ТОН
Спокойный и уважительный, как говорит опытный коллега: по делу, без снисходительности и без восторгов. Не «к сожалению» и не «отличная работа!». Короткие фразы. Никаких восклицательных знаков.

ИСХОДНЫЙ ТЕКСТ (JSON)
${JSON.stringify(
  {
    общее: результат.общее,
    пояснения: результат.критерии.map((k) => k.пояснение),
    работает: результат.работает,
    нехватает: результат.нехватает,
    кейсы: результат.кейсы.map((c) => c.описание),
  },
  null,
  1,
)}

ОТВЕТ
Верни ЧИСТЫЙ JSON без markdown и без пояснений, ровно такой структуры:
{
  "общее": "строка",
  "пояснения": ["строка", ...],
  "работает": ["строка", ...],
  "нехватает": ["строка", ...],
  "кейсы": ["строка", ...]
}
Длина каждого массива обязана совпадать с исходной. Пустой массив на входе — пустой на выходе.`;
}
